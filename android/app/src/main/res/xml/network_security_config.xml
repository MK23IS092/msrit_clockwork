<?xml version="1.0" encoding="utf-8"?>
<!--
  Production: HTTPS only. The hosted backend lives on a Hugging Face
  Space (https://*.hf.space) which is HTTPS-only with a valid cert.

  Development: cleartext is allowed for the canonical emulator/desktop
  loopback addresses so contributors can still point at a local FastAPI
  while iterating without flipping flags every time.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
    </domain-config>
</network-security-config>
